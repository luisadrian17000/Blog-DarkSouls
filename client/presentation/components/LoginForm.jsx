import style from './LoginForm.module.css'
import LoginAccessButton from "./LoginAccessButton.jsx";

const LoginForm = () => {
    return (
        <div className={style.mainContainer}>

            <LoginAccessButton />

        </div>
    )}

export default LoginForm;